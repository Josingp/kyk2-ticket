/* ============================================================
   설정 파일 — 회차가 바뀌면 여기만 수정하세요.
   ※ salt 를 바꾸면 db.json 을 반드시 다시 생성해야 합니다. (admin.html)
   ============================================================ */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else root.TICKET_CONFIG = factory();
})(typeof self !== 'undefined' ? self : this, function () {
  return {
    eventName: '신인감독 김연경2',
    eventSub: '직관 초대 티켓',
    eventInfo: '2026. 8. 27. (목) 19:00 · 장충체육관',
    eventDateISO: '2026-08-27',   // 경기일(KST) — 공유 링크 생성 시 안내 문구 분기용

    // 회차 일정(KST). 서버(공유 링크 만료·자동 입장 집계)와 화면 문구가 함께 참조한다.
    schedule: {
      doorsOpen: '2026-08-27T17:30:00+09:00',   // 입장 시작
      gameStart: '2026-08-27T19:00:00+09:00',   // 경기 시작
      end:       '2026-08-27T22:30:00+09:00',   // 행사 종료 기준 — 공유 링크는 최소 이 시각까지 유효
    },
    scheduleText: '17:30 입장 시작 · 19:00 경기 시작',

    // 티켓 하단 고지 문구
    noticeLines: [
      '* 본 티켓은 〈신인감독 김연경2〉 관계자를 통해 초대받은 수신자 본인만 사용 가능하며, 타인 사용 시 입장이 불가합니다.',
      '무단 유출 및 공유 시 법적 책임이 발생할 수 있습니다.',
    ],

    // 회차 고유값 — 회차(공연일)마다 다르게 설정. Redis 키·암호화 솔트에 쓰여 회차별로 완전히 분리 저장된다.
    //   8/20 잠실학생체육관 : 'KYK2-JAMSIL-0820'  (데이터는 관리자 화면에서 회차 키를 입력해 파기할 때까지 보관)
    //   8/27 장충체육관     : 'KYK2-JANGCHUNG-0827'
    salt: 'KYK2-JANGCHUNG-0827',

    // 현장 위치 인증 — 관람객 화면 배지 + 위치 기반 자동 입장 처리의 기준점
    venue: {
      name: '장충체육관',
      lat: 37.5580,
      lng: 127.0060,
      radiusM: 300,
    },

    /* 위치 기반 자동 입장 처리
       - 티켓 화면이 열려 있는 동안 기기가 행사장까지의 거리만 계산해(좌표는 보내지 않음)
         반경 안일 때 상태 조회와 함께 서버에 알린다.
       - 실시간 규칙: collectFrom 이후 반경 안 보고가 minReports 회 이상이고 첫 보고부터 minMinutes 분 이상
         지속되면, 그 기기에 표시 중인 미입장 좌석을 '자동 입장'으로 기록한다 (applyFrom 이후에만).
       - 자동 입장은 스태프 입장과 구분 표시되며, 게이트에서 스태프가 누르면 정식 입장으로 바뀐다.
       - 종료 후 보정: 관리자 화면 「위치 기반 미검표 보정」에서 남은 미입장 좌석의 체류 기록을 보고 선택 처리. */
    autoCheckin: {
      enabled: false,   /* 위치 기반 자동 입장 사용 안 함 — 입장 처리는 스태프 [입장 확인] 버튼으로만 */
      radiusM: 300,        // 이 거리(m) 안의 위치 보고만 집계
      maxAccM: 200,        // GPS 정확도(m)가 이보다 나쁜 보고는 무시
      minReports: 3,       // 실시간 자동 입장: 반경 안 보고 3회 이상 …
      minMinutes: 1,       // … 이고 첫 보고부터 1분 이상 체류
      collectFrom: '2026-08-27T16:00:00+09:00',   // 이 시각부터 집계 (입장 시작 1시간 30분 전)
      applyFrom:   '2026-08-27T17:30:00+09:00',   // 자동 입장 처리는 입장 시작 이후에만
      until:       '2026-08-27T22:30:00+09:00',   // 이 시각 이후 보고는 집계하지 않음
    },

    // 암호화 강도(키 유도 반복 횟수). 변경 시 db.json 재생성 필요.
    iterations: 100000,
  };
});
